# 01. 시작하기

## 선수 지식 점검

- Python: 함수, class, NumPy array와 notebook 실행
- 선형대수: vector·matrix 곱, transpose, inverse, eigenvalue
- 미적분: 편미분, gradient, chain rule
- 확률: 조건부확률, Bayes rule, expectation, variance

모두 완벽히 알아야 시작할 필요는 없습니다. 생소한 항목을 표시하고 강의에서 다시 등장할 때 보충합니다.

## 8주 기본 경로

1. ML 문제 유형, data·feature·label 구분
2. 선형회귀와 loss, gradient descent
3. logistic regression과 분류 metric
4. 일반화, train/test split, bias-variance
5. tree, k-NN, kernel과 SVM
6. neural network와 backpropagation
7. clustering, PCA, mixture model
8. 작은 dataset으로 전 과정 재현 및 회고

## 매주 반복할 루틴

1. 번역 노트에서 이번 주 범위와 용어를 확인합니다.
2. 공식 원문을 읽고 수식의 가정과 기호를 직접 적습니다.
3. 핵심 algorithm을 작은 synthetic data에 구현합니다.
4. library 구현과 결과를 비교합니다.
5. 실패 사례와 다음 질문을 [학습 기록](templates/course-study-log.md)에 남깁니다.
